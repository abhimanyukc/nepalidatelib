name: Node.js CI

on: [push, pull_request]

jobs:
  build:

    runs-on:  windows,ubuntu-latest

    strategy:
      matrix:
        node-version: [20.9.0]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.20.9.0 }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.20.9.0 }}
    - run: npm install
    - run: npm test
